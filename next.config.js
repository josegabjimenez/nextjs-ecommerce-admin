/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['placeimg.com', 'media.istockphoto.com', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
